# 当前未完成事项

> 更新时间：2026-03-18
> 范围：当前 Web / Electron 框架、ini 导入映射功能完成后的剩余事项

## 1. 尚未完成

### 1.1 Windows EXE 打包验证

- 关联计划项：`TODO-C7`
- 当前状态：
  - `npm run build` 已通过
  - Web 构建已通过
  - Electron TypeScript 构建已通过
  - `npm run build:desktop` 在当前环境中执行超时
- 含义：
  - 桌面端打包链路已经配置完成
  - 但最终的 Windows 安装包或最终可执行产物还没有完成端到端确认
- 下一步：
  - 重新执行 `npm run build:desktop`
  - 确认生成的安装包或解压产物是否可以正常启动

## 2. 仍待确认的决策

### 2.1 内置 Stata 模板覆盖范围

- 关联规格项：`Open Questions`
- 当前状态：
  - 模板管理能力已经存在
  - 当前内置模板只有：
    - `stata18-modern`
    - `stata17-classic`
- 尚未确认：
  - 首版需要预置哪些 Stata 版本模板
- 下一步：
  - 明确首版内置模板列表，例如是否需要覆盖 Stata 15 / 16 / 17 / 18

## 3. 已实现但仍属于骨架阶段

以下内容已经具备基础实现，但目前仍属于骨架或首版能力，不是完整产品深度：

- ini 导入：
  - 已支持版本化结构和别名映射
  - 已支持导入报告
  - 但更完整的兼容性处理和更严格的分节校验还未继续扩展
- 模板管理：
  - 已支持模板选择
  - 已支持用户模板保存和删除
  - 但“模板内容可视化编辑”流程尚未实现
- 脚本生成：
  - 已具备可运行的生成器
  - 已覆盖当前支持的方法
  - 但仍属于首版模板生成器，尚未发展成完整论文方法库

## 4. 验证缺口

当前实现主要验证到了“可以构建通过”这一层，以下内容仍缺少专门测试：

- ini 解析与别名兼容
- 导入警告和未知字段展示
- 最近一次配置持久化行为
- 用户模板持久化行为
- Web 与桌面端输出一致性
- Windows 打包行为

## 5. 建议的后续顺序

1. 完成 `TODO-C7`，收掉 Windows EXE 打包验证。
2. 确认首版内置的 Stata 版本模板范围。
3. 增加 ini 导入、模板持久化、脚本生成结果的针对性测试。
4. 决定是否继续扩展到“可编辑模板内容”的版本管理能力。
